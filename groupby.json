{
  "name": "Group",
  "id_name": "groupby",
  "description": "Aggregate using multiple columns, including dates. Set the garnulirity of date groups, and caclulate sum, averages, or other typical operations. .",
  "category" : "Analyze",
  "icon": "group",
  "help_url":"modules/analyse/group",
  "param_schema": {
    "groups": {
      "type": "dict",
      "properties": {
        "colnames": { "type": "multicolumn" },
        "group_dates": { "type": "boolean" },
        "date_granularities": {
          "type": "map",
          "value_dtype": { "type": "string" }
        }
      }
    },
    "aggregations": {
      "type": "list",
      "inner_dtype": {
        "type": "dict",
        "properties": {
          "operation": { "type": "string" },
          "colname": { "type": "column" },
          "outname": { "type": "string" }
        }
      }
    }
  },
  "parameters": [
    {
      "id_name": "groups",
      "type": "custom"
    },
    {
      "id_name": "aggregations",
      "type": "custom"
    }
  ]
}
