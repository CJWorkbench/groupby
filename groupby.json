{
  "name": "Group",
  "id_name": "groupby",
  "description": "Group rows by columns (also called pivot table). Count rows and aggregate values within each group, and caclulate sum, averages, Min, Max and more between columns.",
  "category" : "Analyze",
  "icon": "group",
  "help_url":"modules/analyse/group",
  "param_schema": {
    "groups": {
      "type": "dict",
      "properties": {
        "colnames": { "type": "multicolumn" },
        "group_dates": { "type": "boolean" },
        "date_granularities": {
          "type": "map",
          "value_dtype": { "type": "string" }
        }
      }
    },
    "aggregations": {
      "type": "list",
      "inner_dtype": {
        "type": "dict",
        "properties": {
          "operation": { "type": "string" },
          "colname": { "type": "column" },
          "outname": { "type": "string" }
        }
      }
    }
  },
  "parameters": [
    {
      "id_name": "groups",
      "type": "custom"
    },
    {
      "id_name": "aggregations",
      "type": "custom"
    }
  ]
}
